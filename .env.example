# ============================================
# Gemini Business2API 配置示例
# ============================================

# ----------------------------
# 访问控制（必需）
# ----------------------------
# 管理员密钥：用于登录管理面板（必填）
ADMIN_KEY=your-admin-secret-key

# Session 密钥：用于登录态签名（强烈建议固定，避免重启后登录态失效）
# 示例：openssl rand -hex 32
# SESSION_SECRET_KEY=your-session-secret-key

# ----------------------------
# 路径前缀（推荐）
# ----------------------------
# 将整个应用挂载到 "/<PATH_PREFIX>/*" 下（包括 WebUI /admin /v1 /public 等）。
# 典型用途：配合反向代理仅放行前缀路径，降低暴露面。
#
# 注意：启用后，请使用 https://host/<PATH_PREFIX>/ 访问 WebUI。
# PATH_PREFIX=your-random-path-prefix

# ----------------------------
# 运行端口（可选，默认 7860）
# ----------------------------
# PORT=7860

# ============================================
# 数据库配置（可选，用于无持久化存储的环境如 HF Spaces）
# ============================================
# 支持 PostgreSQL 数据库存储（账户/设置/统计）
# 未设置时使用本地文件存储（原有行为）
#
# 示例（Neon PostgreSQL）：
# DATABASE_URL=postgresql://user:password@ep-xxx.aws.neon.tech/dbname?sslmode=require
#
# 注意：使用数据库存储需要安装 asyncpg：pip install asyncpg
# DATABASE_URL=

# ============================================
# CORS（可选）
# ============================================
# 仅在你将 WebUI 部署在不同域名/端口时需要配置。
# FRONTEND_ORIGIN=https://your-frontend.example.com
# ALLOW_ALL_ORIGINS=0

# ============================================
# 其他配置请在管理面板的"系统设置"中配置
# 包括：API密钥、代理、图片生成、重试策略等
# 配置保存在 data/settings.yaml
# ============================================

# ============================================
# 账户配置
# ============================================
# 使用 accounts.json 文件
# 账户配置保存在 accounts.json 文件中
# 首次启动时会自动创建空配置
# 请在管理面板中添加账户，或直接编辑 accounts.json

# 高级：不要设置 ACCOUNTS_CONFIG（会覆盖 accounts.json / 数据库存储，导致 WebUI 看起来不生效）
# ACCOUNTS_CONFIG=
